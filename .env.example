# Camera and proxy settings
CAMERA_HOST=
CAMERA_USERNAME=
CAMERA_PASSWORD=
CAMERA_TIMEOUT_MS=5000
# Optional: which camera stream/profile to request (go2rtc defaults to channel 1)
CAMERA_STREAM_ID=1

# Cloudflare Tunnel (required for public access)
TUNNEL_CREDENTIALS_FILE=./cloudflared/tunnel.json
TUNNEL_NAME=your-tunnel-name
TUNNEL_ORIGIN_CERT=your-cert-file
