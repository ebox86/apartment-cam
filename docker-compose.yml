version: "3.8"

services:
  cam-proxy:
    build: ./cam-proxy
    container_name: apartment-cam-proxy
    environment:
      CAM_URL: "http://10.0.0.42/axis-cgi/mjpg/video.cgi"
    ports:
      - "3001:3000"
    restart: unless-stopped
